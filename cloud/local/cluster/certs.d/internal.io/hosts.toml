server = "http://registry-service.registry-internal.svc.cluster.local"

[host."http://registry-service.registry-internal.svc.cluster.local"]
capabilities = ["pull", "resolve", "push"]
skip_verify = true